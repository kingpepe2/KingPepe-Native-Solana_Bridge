# Shared utilities scaffold

Planned scope:

- Canonical codecs and domain models shared by Native and Solana layers.
- Value-level helpers with explicit bounds and precision checks.
- Cross-service serialization compatibility types.
