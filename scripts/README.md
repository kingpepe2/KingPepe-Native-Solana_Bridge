# Validation scripts

Commands are defined in package.json and the CI workflow. They cover protocol vectors, isolated chain execution, restart, platform regressions, dependency/license checks, source provenance and publication policy. Use pinned tools and fresh external test directories. Passing source checks does not authorize blockchain transactions.
