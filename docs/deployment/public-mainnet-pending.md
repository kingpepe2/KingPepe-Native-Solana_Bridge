# Public Mainnet Bridge — activation pending

The current production presentation is KingPepe Native MAINNET → Solana MAINNET, ONE WAY, automatic Native burn → exact 1:1 KPEPE mint. The official existing Mint is `4QkWKqTMyPEyEb8RMKv3XrcHJ5jbS7k4uQhXVoirphZW`: standard SPL Token, 8 decimals, zero initial supply and no freeze authority. This cleanup creates no Mint or on-chain account and spends no SOL.

The page prominently displays the Mint, a copy control, its Mainnet explorer link and **MAINNET BRIDGE — ACTIVATION PENDING**. **DO NOT SEND KPEPE YET.** The eight-step transfer explanation is labelled as the production flow after activation. Wallet connection is disabled to avoid implying that deposits are accepted. There is no approval, reverse, withdrawal or payout control. Official Website, Bridge, X and GitHub links match the canonical metadata.

The public status reports `network=MAINNET`, `direction=NATIVE_TO_SOLANA`, `mode=BURN_AND_MINT`, `activation=PENDING`, `depositsAccepted=false`, `productionReady=false` and `mainnetActivation=DISABLED`. A distinct private pending profile selects a backend with no wallet, signer, loopback access token, operation-creation or transaction capability. Only status is available. Operation creation, saved-operation retrieval and address/balance paths return `503 ACTIVATION_PENDING`; unknown signing/admin/reverse routes remain absent. Origin, request-size, rate and method controls remain in place.

The pending counter is **0 / 21,000,000 KPEPE** only while fresh finalized Mainnet reads verify the existing Mint's token-program owner, decimals, zero supply, fixed Bridge mint-authority PDA and absent freeze authority. The credential-free public RPC is called only server-side with two fixed read methods and the fixed official Mint. Its endpoint and request fields cannot be supplied by a browser. A ten-second cache coalesces status reads; failures invalidate the value. The baseline has a separate source identity, `MAINNET_PREACTIVATION_ZERO_BASELINE`, which the active accounting parser cannot accept as completed burn/mint accounting. A nonzero Mint supply causes this zero-only backend to withhold the counter until deployed canonical accounting is selected.

The retained private development runtime, wallets, completed operations, journals, fixtures and historical evidence are preserved. Their balances cannot enter this Mainnet status. The restricted Explorer HTTP process and the separate protected Native signer boundary are unchanged.

Gateway regression covers pending-mode economic rejection, saved TEST-operation isolation, wrong-cluster/Mint/authority/supply rejection, stale baselines and unchanged active-runtime admission. Public HTTP and deployed-source checks establish transport, status, assets and presentation code. They do not establish manual browser rendering or Phantom acceptance; those remain PENDING_MANUAL when no connected browser is available.

Explorer source `daa7d6e103ff84753d2d31a4f42293924e02a57c` passed 35 affected tests and source/staged/outgoing secret scans. Its reviewed public deployment passed 24 HTTP checks, including Home, Bridge, three CSS reads, exact served-file hashes, Mainnet pending status, zero baseline, blocked operation creation and saved-operation retrieval, absent reverse/signing/admin routes, and unchanged metadata. Only the Explorer HTTP child restarted; its existing restricted-token launcher and supervisor were preserved. Unrelated live navigation and source changes were retained. No connected-browser visual or Phantom PASS is claimed.

After programs/configuration, signer, attestation, journal, operational funding and controlled Mainnet burn/mint verification pass, replace the pending profile with the reviewed authenticated production gateway. Verify canonical production accounting, original recipient and exact supply delta before normal deposit acceptance. The pending page cannot activate itself, and no configuration boolean bypasses deployed-program or controlled-operation checks. Refresh the page after the verified production cutover.
