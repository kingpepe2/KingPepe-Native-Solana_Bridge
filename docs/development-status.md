# Current Mainnet deployment status

The current public production presentation is **MAINNET BRIDGE — ACTIVATION PENDING**: KingPepe Native MAINNET → irreversible Native burn → exact 1:1 Solana MAINNET mint. The existing official Mint is shown prominently. Public deposits, wallet connection and address issuance are disabled until final Program/config deployment and controlled activation pass. The separate pending status backend verifies only the Mainnet zero-supply baseline and has no economic capability. [Public pre-activation behavior](deployment/public-mainnet-pending.md) describes this boundary.

The reviewed Explorer implementation passed 35 affected gateway, wallet and supply tests. Browser/Phantom manual acceptance remains pending. Historical REGTEST/DEVNET proofs below retain their original networks, amounts and source identities; their counter is never imported into production.

The [2026-09-25 full source audit](deployment/source-audit-2026-09-25.md) found and fixed pre-funded Solana account handling and its completion-verifier bound. The corrected Transceiver is 181,472 bytes with a new hash; earlier artifact/funding figures below are retained historical evidence. Refresh the frozen release and funding quote, and require exact-SHA CI before deployment. No Mainnet transaction was sent during the audit.

## Retained source-bound implementation and deployment evidence

The Team-authorized metadata update preserves the existing Mainnet Mint, metadata account/URI and approved logo. The canonical JSON now includes the exact burn-and-mint description, project website, Bridge, X and GitHub links. All four destinations returned HTTP 200; three fresh metadata reads matched the published hash. Finalized before/after queries confirmed unchanged Mint and metadata account bytes, 8 decimals, zero supply, Bridge-PDA mint authority and no freeze authority. No on-chain update or SOL expenditure was required. The GitHub description now uses burn-and-mint wording; current deployment status remains explicit below.

The current non-funding implementation provides a protected Mainnet runtime, durable PREPARED/CONTROLLED/NORMAL admission, Mainnet Wallet Standard status and existing-Mint-only config initialization. The private limited-account host passed the actual automatic REGTEST/local-validator flow and crash/restart recovery. Production custody, protected RPC imports, key reopening, the empty paused journal and actual Mainnet network identities are verified. Explorer's public child now runs without enabled administrator privileges. Production remains disabled pending program/config deployment, operational funding, final deployment binding and controlled activation. See [runtime deployment support](deployment/mainnet-runtime.md) for the evidence scope.

The Team removed mandatory Windows Service/SCM hosting and offline recovery provisioning. `OFFLINE_RECOVERY_BACKUP = NOT_REQUIRED_BY_TEAM_DECISION`; `RECOVERY_RISK = ACCEPTED_BY_KINGPEPE_TEAM`. No offline backup or server-loss recovery is claimed. The old SCM proof service is not a dependency of the verified private process host. Each published implementation milestone requires its own exact-SHA CI.

Architecture: ONE_WAY_AUTOMATIC_BURN_AND_MINT. Source verification, isolated Native burn proof, real REGTEST -> DEVNET burn/mint and crash-after-burn recovery passed at their recorded source identities. The retained TEST deployment and its evidence are historical; the current public target is Mainnet activation pending. The Native RPC cookie fix passed its exact-SHA CI and reviewed read-only recovery; the TEST service is healthy with MATCH reconciliation. Browser acceptance remains pending. Mainnet deployment has started with the zero-supply Mint and metadata; programs and activation remain pending. productionReady=false and mainnetActivation=DISABLED.

Runtime source `adcca2ba412ed45266041d73b16207f665a55c34` passed all four required jobs and every step in CI 35931741887. [Current burn TEST evidence](deployment/burn-devnet-report.md) records the initial finalized-read race, its fix, the same-operation read-only recovery, and the subsequent fresh successful crash-recovery operation. The [new TEST manifest](deployment/burn-devnet.json) identifies actual programs/Mint and artifact hashes. Historical reserve evidence certifies only its named source and remains labelled.

Current TEST runtime source `cb5a6af1691366443d69fb3a1f5450a0b6cdc751` passed CI 35960715676: all four jobs and every step, none skipped. It adds Native cookie restart recovery without changing program artifacts or economic encoding. Recovery independently reverified both original completed burns and mints, then resumed the existing TEST journal with no new deposit, burn, mint or transaction RPC. This evidence-only publication requires its own exact-SHA CI.

The new private TEST service reconciles two completed operations totaling 200,000 base units (0.002 TEST KPEPE). Each deposited, burned and minted exactly 100,000 base units, with a separate 235-base-unit Native miner fee. Actual signed Borsh bytes match Rust, TypeScript and finalized on-chain receipts. An encrypted same-account TEST restore passed; it does not certify a replacement Windows principal or offline production recovery.

Current source contains no active FROST or permanent reserve economy. Explorer commit `294ee10b07c430e8a3ae0c03c7c850f1a9e1d45a` supplies the deployed wallet-first burn UI; 29 affected Node tests and source/staged/outgoing secret scans passed. Windows elevation was approved and only the ten reviewed Bridge files/private TEST gateway were applied; unrelated live files were preserved. Public HTTP/API checks and served-file hashes passed, with 0.002 TEST KPEPE accounted. The historical reserve/FROST TEST service was retired gracefully; its private state remains preserved. Desktop/mobile rendering is unverified because no browser is connected. Phantom remains PENDING_MANUAL.

The Native TEST node exited during retirement of the old WSL session. Its original chain was restarted under an independent retained launcher. Cookie rotation exposed a cached-authentication defect: the running RPC client sent the previous cookie, received HTTP 401 and persisted a safety pause. The correction re-reads the validated cookie reference for ordinary and batched requests. Missing cookies or an authentication race hold processing without retrying a broadcast; malformed/path-rejected cookies still fail closed. The real REGTEST proof now retains the same RPC client and verifier across restart and reindex. Existing critical pauses are not cleared by this change.

After source review and CI, the quiescent TEST stores were backed up privately, the service was gracefully drained, and the corrected source was applied. Read-only chain checks confirmed unchanged economic records before the local reviewed recovery cleared this specific pause. Current public HTTP/API checks passed for Home, Bridge, existing Explorer routes, exact served bytes, counter, balances and reverse-route rejection. Back-to-back uncached balance requests correctly hit the existing one-second throttle; the diagnostic reproduced the retry condition and paced reads passed without weakening the limit. No connected-browser visual or manual Phantom PASS is claimed.

The Team subsequently authorized initial Mainnet deployment and controlled activation. Production runtime activation remains disabled. Mainnet Native adapters now bind the canonical genesis/domain and retain independently verified input/burn block locations for nodes without a transaction index. Mainnet Solana transport accepts only the protected launcher's endpoint and checks cluster identity around every request. TEST relabeling does not satisfy Mainnet identity, and the TEST resume method cannot enable a Mainnet runtime.

The unsigned standard-SPL metadata setup creates the zero-supply Mint, creates KingPepe/KPEPE metadata and assigns the exact Bridge mint PDA in one atomic transaction. A separate transaction initializes the paused Bridge/Transceiver configuration. The temporary creation authority cannot survive a successful transaction as an alternate mint path. The public metadata update authority is the protected enrollment identity; it is distinct from SPL mint authority and the dedicated upgrade authority. The original setup and canonical program encodings remain unchanged.

Fresh production-target v3 builds reproduce the reviewed program hashes: Bridge 161,680 bytes and Transceiver 178,712 bytes. Current Mainnet rent/message-fee queries and actual Mint/metadata simulation produce a 1.795339560 SOL budget including metadata, controlled activation accounts and bounded initial-operation/retry headroom. The deployed Metaplex program allocated 607 bytes and charged a 0.01 SOL creation fee; upstream main predicted different values, underquoting this component by 0.002720000 SOL. The simulation, not unpublished upstream state, determines the corrected budget. This is a source/artifact-specific quotation, not a claim of deployment. A sanitized funding ledger tracks actual receipts and transactions privately. Never publish prepared addresses as official deployed identities before on-chain verification.

Private signer isolation, automatic execution in the isolated regression environment, protected production custody and the paused production journal were subsequently verified as described above. Operational Native funding, the final minimum/refill policy, remaining Mainnet programs/configuration and controlled activation remain pending. The old SCM proof service and offline provisioning are not activation controls. Loss of the only usable burn key may leave confirmed but unburned deposits unavailable for automatic processing/recovery, which the Team accepts. The public Bridge now presents Mainnet activation pending and accepts no deposits. Historical REGTEST/DEVNET evidence is retained separately. Mainnet economic processing must not be enabled until controlled activation and all pre-burn prerequisites pass. See BRIDGE-READINESS.json and the production execution plan.

## Initial Mainnet Mint deployment

Source `f5d6c34948a153777016ae41a8a8a673d5d9c4e0` passed exact-SHA CI `36022276097`: four jobs, 65 steps, none skipped. The frozen Solana program source tree and artifact hashes remained unchanged. Final Mainnet simulation, retained protected identity verification and clean-source checks passed before the authorized transaction.

On 2026-09-24, transaction `4C39PNE3EKZYckYctz12tkxNo3wjK59ySiq4fmgQLDLLX4uAEVBRq1Zji35LTAVpkPEZUHvA21iyNoaFuCvbFUv7` finalized at slot 450075833. It atomically created the standard-SPL KPEPE Mint, its approved metadata, and the Bridge-PDA Mint authority binding. Independent finalized account and token-supply queries verified 8 decimals, zero supply and no freeze authority. Public identities and costs are recorded in [mainnet.json](deployment/mainnet.json).

The payer received 0.051667147 SOL and spent 0.014810600 SOL: 0.004800600 account/rent funding, 0.010000000 metadata protocol fee and 0.000010000 transaction fee. Its remaining 0.036856547 SOL is insufficient for the next Bridge program buffer initialization. The current minimum additional balance for that transaction, preserving the payer rent floor, is 0.786016933 SOL; completing the whole Bridge program needs 0.788635053 SOL additional. The remaining full deployment/initial-operation budget is 1.780528960 SOL, or 1.743672413 SOL additional. These quotes must be rechecked before the next transaction. No program, config, Native burn or controlled mint is represented as completed by this partial deployment.
