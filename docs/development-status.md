# Current development status

Architecture: ONE_WAY_AUTOMATIC_BURN_AND_MINT. Source verification, isolated Native burn proof, real REGTEST -> DEVNET burn/mint, crash-after-burn recovery and exact-source CI passed. Public TEST cutover remains blocked by Windows elevation approval and browser acceptance. Mainnet is not deployed or activated; productionReady=false and mainnetActivation=DISABLED.

Runtime source `adcca2ba412ed45266041d73b16207f665a55c34` passed all four required jobs and every step in CI 35931741887. [Current burn TEST evidence](deployment/burn-devnet-report.md) records the initial finalized-read race, its fix, the same-operation read-only recovery, and the subsequent fresh successful crash-recovery operation. The [new TEST manifest](deployment/burn-devnet.json) identifies actual programs/Mint and artifact hashes. Historical reserve evidence certifies only its named source and remains labelled.

The new private TEST service reconciles two completed operations totaling 200,000 base units (0.002 TEST KPEPE). Each deposited, burned and minted exactly 100,000 base units, with a separate 235-base-unit Native miner fee. Actual signed Borsh bytes match Rust, TypeScript and finalized on-chain receipts. An encrypted same-account TEST restore passed; it does not certify a replacement Windows principal or offline production recovery.

Current source contains no active FROST or permanent reserve economy. Explorer commit `294ee10b07c430e8a3ae0c03c7c850f1a9e1d45a` prepares the simple wallet-first burn UI; 29 affected Node tests and source/staged/outgoing secret scans passed. The reviewed Windows UAC request was cancelled/rejected, so live Explorer remains unchanged. Its historical reserve/FROST TEST service and journal remain preserved pending the reviewed cutover. Desktop/mobile rendering is unverified because no browser is connected. Phantom remains PENDING_MANUAL.

Mainnet execution admission is still TEST-only. Dedicated burn service identity, portable encrypted offline recovery, production burn/fee/journal/recovery bindings, operational funding/minimum policy and metadata hosting remain readiness work. No Mainnet key or Mint was created by this conversion. No funding or activation approval is requested. See BRIDGE-READINESS.json and the production preparation plan.
