# Solana scripts scaffold

Purpose:

- Store scripted operations for deterministic CI/build helpers.
- Keep service entry commands separate from source crates.

Examples planned for later phases:

- Program upgrade and deployment checks.
- Runtime manifest checks.
- Smoke transaction generation for localnet flows.
