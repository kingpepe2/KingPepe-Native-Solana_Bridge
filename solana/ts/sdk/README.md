# Solana bridge SDK scaffold

Purpose:

- Provide typed account and instruction helpers for bridge operations.
- Support deposit intent construction and withdrawal request inspection.

Expected future shape:

- Config-aware client initialization.
- Program + mint identity validation helpers.
- Deterministic state decoding.
