# TS SDK library scaffold

Planned content:

- Shared generated client types for bridge messages.
- Canonical constants for account fields and status enums.
- Nonce-safe helpers for request state transitions.

No signing keys or backend credentials are stored in this folder.
